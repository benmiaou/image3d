#include "voxel.hpp"

voxel::voxel(pgm3D pgm)
{
   /* int l,c,d,max;
    pgm.getInfo(&l,&c,&d,&max);
    vector<int> values = pgm.getValues();
    for(int x = 0; x < l; x++)
        for(int y = 0; y<c; y++)
            for(int z =0; z<d; z++)*/

}
